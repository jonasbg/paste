deployment:
  repository: ghcr.io/jonasbg/droply
  pullPolicy: Always
  tag: "latest"
  deployment: deployment
  mounts:
  - name: uploads
    mountPath: /uploads
    readOnly: false
    storage: 10Gi
  - name: data
    mountPath: /data
    readOnly: false
    storage: 500Mi
  host: chase.example.com
  env:
  - name: UPLOAD_DIR
    value: "/uploads"
  - name: DATABASE_PATH
    value: "/data"
  services:
  - name: http
    port: 8080